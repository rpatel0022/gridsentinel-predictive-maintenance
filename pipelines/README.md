# pipelines/

Feature-engineering and training workflows (Phase 1+): the reproducible path from
raw MetroPT-3 / Backblaze data → validated features → tracked model runs. Includes
the data-validation suite (pandera/Great Expectations) and temporal/grouped CV.
