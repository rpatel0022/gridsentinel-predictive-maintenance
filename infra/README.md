# infra/

Deployment and infrastructure (Phase 5+): AWS ECS/Fargate + S3 definitions, the
live-ingestion service for the EIA feed, secrets wiring (env/secret-manager —
zero keys in git), and the edge/ONNX quantization target.
