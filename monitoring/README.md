# monitoring/

Observability and self-healing (Phase 4+): Prometheus + Grafana two-tier
dashboards (system *and* model metrics), Evidently drift monitors on leading
indicators, the delayed-label backfill job, and the drift → retrain → canary →
promote → rollback loop, plus the on-call runbook.
